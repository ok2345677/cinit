#!/bin/sh
# crond — cron-планировщик задач
#  Arch/systemd:  systemctl enable --now cronie.service
#  Void:          cronie пакет; на Arch бинарь /usr/bin/cronie-crond
case $1 in
start) exec cronie-crond -n ;;
stop) pkill cronie-crond ;;
esac