#!/bin/sh
# sshd — OpenSSH-сервер
#  Arch/systemd:  systemctl enable --now sshd.service
#  Void:          openssh пакет (включает /usr/bin/sshd)
case $1 in
start)
	ssh-keygen -A >/dev/null 2>&1
	exec /usr/bin/sshd -D
	;;
stop) pkill sshd ;;
esac