#!/bin/sh
# nginx — веб-сервер
#  Arch/systemd:  systemctl enable --now nginx.service
#  Void:          nginx пакет; daemon off — чтобы init ждал его (не форк)
case $1 in
start) exec nginx -g 'daemon off;' ;;
stop) pkill nginx ;;
esac