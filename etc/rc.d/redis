#!/bin/sh
# redis — in-memory БД
#  Arch/systemd:  systemctl enable --now redis.service
#  Void:          redis пакет; юзер redis создан пакетом
case $1 in
start)
	install -d -m0750 -o redis -g redis /run/redis
	exec redis-server /etc/redis/redis.conf
	;;
stop) pkill redis-server ;;
esac