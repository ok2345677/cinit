#!/bin/sh
# mounts — монтирование fstab (mount -a) + подкачка
#  Arch/systemd:  systemd делает сам при старте (local-fs.target)
#  Void:          этот скрипт; stop = выключение подкачки
case $1 in
start)
	mount -a
	swapon -a
	;;
stop) swapoff -a ;;
esac