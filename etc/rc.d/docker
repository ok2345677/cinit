#!/bin/sh
# docker — контейнеры Docker
#  Arch/systemd:  systemctl enable --now docker.service
#  Void:          docker пакет; dockerd сам форкает containerd
case $1 in
start)
	modprobe -q loop
	exec dockerd
	;;
stop)
	pkill dockerd
	pkill containerd
	;;
esac